umask 022
fsck -a /
setdate
remount -n / rw
prtroot >/etc/mtab
# 8 MiB PSRAM disc as swap (harmless error if no PSRAM fitted)
swapon /dev/hdc 16384
#Uncomment if you have multiple file systems
#fsck -a
#mount -a
>/var/run/utmp
